name: Deploy Alpine Korea Homepage

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Make Scripts Executable
        run: chmod +x ./scripts/*.sh || true

      - name: Execute Blue-Green Deployment
        run: ./scripts/deploy.sh
